# Docker Compose reads this file for variable substitution.
# Copy to .env and fill in your values:
#   cp .env.example .env

# Clerk publishable key (same value as CLERK_PUBLISHABLE_KEY in backend/.env)
# Required: passed to the frontend at Docker build time
CLERK_PUBLISHABLE_KEY=pk_test_...
