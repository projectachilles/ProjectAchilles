[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPatterns = ["src/**", "public/**", "package.json", "package-lock.json", "tsconfig.json", "Dockerfile", "nginx.conf", "docker-entrypoint.sh"]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
